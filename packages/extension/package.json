{
  "name": "@x-to-obsidian/extension",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "bun run build:background && bun run build:content && bun run build:popup && bun run build:copy",
    "build:background": "bun build src/background.ts --outdir=dist --target=browser",
    "build:content": "bun build src/content.ts --outdir=dist --target=browser",
    "build:popup": "bun build src/popup/popup.ts --outdir=dist/popup --target=browser",
    "build:copy": "cp manifest.json dist/ && cp -r src/popup/popup.html dist/popup/ && cp -r src/icons dist/"
  },
  "dependencies": {
    "@x-to-obsidian/core": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "^1.1.42",
    "@types/chrome": "^0.0.287"
  }
}
